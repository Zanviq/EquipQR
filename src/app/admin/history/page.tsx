import { prisma } from "@/server/db/client";

const labels: Record<string, string> = { CHECKOUT: "대여", RETURN: "반납", TRANSFER: "전달", TRANSFER_TICKET_CREATED: "전달 QR 생성", TRANSFER_TICKET_CANCELLED: "전달 QR 취소", EQUIPMENT_CREATED: "장비 등록", EQUIPMENT_UPDATED: "장비 수정", ADMIN_REASSIGN: "관리자 재배정", ADMIN_RECALL: "관리자 회수", STATUS_CHANGED: "상태 변경", USER_CREATED: "사용자 등록", USER_UPDATED: "사용자 변경", PASSWORD_RESET: "비밀번호 초기화" };

export default async function AdminHistoryPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) {
  const { q = "" } = await searchParams;
  const events = await prisma.auditEvent.findMany({ where: q ? { OR: [{ equipment: { assetNumber: { contains: q, mode: "insensitive" } } }, { equipment: { name: { contains: q, mode: "insensitive" } } }, { actorUser: { name: { contains: q, mode: "insensitive" } } }, { actorUser: { employeeNumber: { contains: q, mode: "insensitive" } } }, { previousUser: { name: { contains: q, mode: "insensitive" } } }, { previousUser: { employeeNumber: { contains: q, mode: "insensitive" } } }, { nextUser: { name: { contains: q, mode: "insensitive" } } }, { nextUser: { employeeNumber: { contains: q, mode: "insensitive" } } }] } : undefined, take: 200, orderBy: [{ occurredAt: "desc" }, { id: "desc" }], include: { equipment: true, actorUser: true, previousUser: true, nextUser: true } });
  return <main className="admin-wrap stack" style={{ gap: 28 }}><header><p className="eyebrow">Audit trail</p><h1 className="page-title">이용 기록</h1><p className="page-copy">대여·반납·전달과 관리자 변경 이력을 삭제 없이 추적합니다.</p></header>
    <form className="row" role="search"><input className="input" name="q" defaultValue={q} placeholder="장비명, 자산번호, 처리자 이름" aria-label="기록 검색"/><button className="button button-secondary">검색</button></form>
    <section className="data-table-wrap"><table className="data-table"><thead><tr><th>처리 시각</th><th>유형</th><th>장비</th><th>책임 변경</th><th>처리자</th><th>사유</th></tr></thead><tbody>{events.map(event => <tr key={event.id}><td>{event.occurredAt.toLocaleString("ko-KR")}</td><td>{labels[event.eventType] ?? event.eventType}</td><td>{event.equipment ? <><strong>{event.equipment.name}</strong><br/><span className="font-data">{event.equipment.assetNumber}</span></> : "—"}</td><td>{event.previousUser?.name ?? "—"} → {event.nextUser?.name ?? "—"}</td><td>{event.actorUser?.name ?? "시스템"}</td><td>{event.reason ?? "—"}</td></tr>)}</tbody></table>{events.length === 0 ? <div className="empty-state">조건에 맞는 기록이 없습니다.</div> : null}</section>
  </main>;
}
